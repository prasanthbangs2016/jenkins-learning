pipeline {
    agent any

    stages {
        stage('Hai') {
            steps {
                echo "Hello World"
            }
        }
    }
#always : it runs always
    post {
        always {
            echo "I will say hello again"
        }
    }
}